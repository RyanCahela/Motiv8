import React from 'react'

export default function QuoteControls() {
  return (
    <div className="btn-group">
      <button>Keep Quote</button>
      <button>Keep Fonts</button>
      <button>Keep Background</button>
    </div>
  )
}
